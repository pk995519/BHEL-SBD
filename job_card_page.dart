import 'package:flutter/material.dart';

class JobCardPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Job Card"),
      ),
      body: Center(
        child: Text("Job Card Page"),
      ),
    );
  }
}
